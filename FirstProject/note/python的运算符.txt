> 算术运算符
    +(加) -(减) *(乘) /(除) //(整除) **(次方) %(取余)
> 比较运算符
    ==(等于) >(大于) <(小于) >=(大于等于) <=(小于等于) !=(不等于)
> 赋值运算符 (变量 = 数据)(将右侧数据赋值给左边的变量)
    = += -= *= **= /= %= //=
> 逻辑运算符
    and or (相当于&,|)
> 位运算符
    &(与) |(或) ^(异或)
